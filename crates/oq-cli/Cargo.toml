[package]
name = "oq-cli"
version.workspace = true
description = "One entry point for the OpenQuanter tools, so a user has one name to remember instead of nine"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "oq"
path = "src/bin/oq.rs"

[dependencies]

[lints]
workspace = true
