# 变更日志

[English](CHANGELOG.md) · [中文](CHANGELOG.zh-CN.md)

由[版本规则](docs/VERSIONING.zh-CN.md)引用——发布阶段及其承诺定义在那里。本文件
记录**变了什么**，那一篇记录**一个版本号意味着什么**。

两条规则，来自[路线图](docs/ROADMAP.zh-CN.md)的发布节奏与[实施方案](docs/IMPLEMENTATION.zh-CN.md)
§6，它们正是本文件存在的理由：

- **任何对 L0 撮合语义、保证金计算、事件 schema 的改动，都要在这里留下明确条目**，
  并附上展示行为差异的 parity 报告。语义被悄悄改掉，会让此前所有结果都无法归因。
- **Golden 基线只有在 PR 中记录了人工确认后才可重新生成。** 如果下面某条改动动了
  文档引用的数字，它会写明。

`2.0.0-alpha.N` 不对 API 稳定性作任何承诺。这里列出条目是为了让读者看见什么动过，
而不是因为有什么会被优雅地弃用——2.0 之前不存在弃用期。

## 未发布 — 2.0.0-alpha.1

工作区第一次被打上版本号的状态。尚未打 tag，也未发布到 crates.io，所以下面的内容是
"自仓库开始以来"，而不是"自上一个版本以来"。

### 语义与事件模式

记在这里,是因为上面的规则要求如此,而当时**没有遵守**——下面每一条都改变了行为、
却没有留下条目。事后补记比写下来差,比不写好。

- **`Contract::notional` 与 `Contract::unrealized` 改为饱和而非回绕。**
  *会改变结果。* 两者都在 `i128` 里相乘、再用 `as i64` 转换,而那是截断:超出范围的
  名义价值回绕成**负数**,于是落进第一档、维持保证金算成 0、该持仓**永不被强平**。
  由 `FR-MARGIN-7` 的生成输入发现。**行为差异:** 任何"价格 × 数量 × 每跳价值"超过
  `i64::MAX` 的运行,现在有了维持保证金要求(此前没有),并可能在此前不会强平的地方
  强平。范围内的运行不变;仓库里每一个 golden 都在范围内,没有需要重新生成的。
- **`Event::Tick` 现在会指明标的，模式新增 `kind::TICK_ON`(10)。** *对单标的账户
  没有任何变化。* 一个观测必须到达它所属的那个 holding，而账户现在可以持有多个。
  `None` 表示「账户唯一的那个」——这正是此前写下的每一条日志所说的，而且说得属实：
  一个只持有一个标的的账户没有别的答案。持有多个的内核会拒绝它：猜测会把一个标的的
  仓位按另一个标的的价格计价，那是一个错的数字、而不是一个缺失的数字。**行为差异：**
  对只有一个 holding 的账户没有变化。一个变体两个 kind，是因为带标的的 tick 是 68
  字节、不带的是 64——就地加宽载荷会让更早的读者把新记录解码成一条成交量错误的
  tick，而不是拒绝它。前 64 字节逐字节相同，所以一个解码器读两者、由 kind 决定尾巴
  在不在。
- **`Event::Submit` 现在会指明标的，模式新增 `kind::SUBMIT_ON`(11)。** *对单标的账户
  没有任何变化。* 与 `TICK_ON` 同一步、同一个理由：一张不说明自己属于哪个标的的单，
  无法被路由到它所属的 holding——或分片。`None` 表示「账户唯一的那个」，这正是此前
  写下的每一条日志所说的。旧载荷是新载荷的前缀，所以一个解码器读两者。
- **`Event::Funding` 现在会指明标的，模式新增 `kind::FUNDING_ON`(12)。** *对持有多个
  标的的账户会改变结果。* 指明了标的的结算只记到费率所属的那个 holding、用那个
  holding 的合约，不记到别处；持有多个标的时，未指明标的的结算会像未指明标的的 tick
  一样被拒绝——此前它被记到第一个 holding 上，不管费率属于哪个标的。一个平仓的
  holding 现在不付资金费，不论其他 holding 持有什么；此前只有全部 holding 都平仓的
  账户才不付。**行为差异：** 对只有一个 holding 的账户没有变化；此前写下的日志里没有
  这类记录。
- **`Kernel::settle_unreported` 在运行结束时记入撮合器仍扣着未放出的成交，回测会调用
  它。** *对带响应延迟的 L1 与 L2 运行会改变结果。* 在最后一段响应延迟之内产生的
  成交，在交易所那边已经发生，却还没放给账户；于是一次在这段延迟之内结束的运行，
  结果里会少掉它们。**行为差异：** 这类运行现在把这些成交计入成交列表和权益。L0
  什么都不扣，不变。它不是一个事件：日志里没有任何东西会重放「运行结束了」。
- **L1 排队变得对称，零延迟订单也排在倍数队列之后。** *会改变 L1 结果。* 一个没有
  成交的窗口，最高价和最低价都是零；把它们当价格读，每张买单都像被穿过而跳过了
  队列，卖单却从来没有——只落在一侧的乐观。这样的窗口现在不放任何订单穿过队列。
  另外，没有入场延迟的订单，其成交量倍数队列此前按「零成交量」定大小，队列为零，
  报告却仍声称用了倍数；现在按上一个观测定大小。**行为差异：** 在无成交窗口里挂着
  的 L1 买单会更晚成交或不成交，零延迟的 L1 订单会排在真实队列之后。L0 与 L2 实测
  的队列不受影响。
- **乱序的 tick 不再结束回测，也不再让资金费结算两次。** *对时间戳会倒退的数据会
  改变结果。* `FundingSchedule::between` 对倒退的区间不返回任何东西——此前它会切片
  越界并结束运行；运行的资金费标记只向前移动，所以较早与较晚两个 tick 之间那一段
  不会被结算第二次。**行为差异：** 时间戳从不倒退的数据没有变化。
- **`Intent::Limit` 与 `Intent::Market` 现在会指明标的。** 隐含的「这次回调关于
  哪个标的就是哪个」会让同一行代码因为跑在不同回调里而下出不同的单，而调用处什么都
  看不出来。`Context` 会为它所关于的那个标的构造 intent，所以单标的策略永远不用写
  下标的。一张下给本次运行并不持有的标的的单会被拒绝、通过 `on_placed` 告知策略、
  并被计数——绝不会被送给「唯一的那个 holding」，那等于在策略没有看的簿上成交一张
  它没有下过的单。
- **声明"没有成交"的记录不再被解析成一笔成交。** *会改变结果。* 币安在真实成交里
  混发 `"p":"0","q":"0","X":"NA"` 的记录——BTCUSDT 一天 19,725 条，对 540 万条
  真实成交。它们此前被解析成 `Trade { price: 0 }` 并折进 tick，而一个窗口的最低价
  是其中价格的最小值，一个零就把它变成零：真实采集的 1409 分钟里有 1355 分钟最低价
  是 `0.00`，而最高价就在旁边好好的。挂着的限价买单靠最低价触发。**行为差异：**
  凡是从会发这类记录的交易所构建出的 tick 都会变——最低价变成真的，此前被当作
  "第一笔成交之前"丢弃的窗口现在会被输出。生成夹具里没有这类记录，所以仓库里每一个
  golden 都不变。这条规则现在是适配器契约(`parse_trade` 要么返回价量为正的 `Trade`，
  要么返回 `None`)，`conformance` 会强制每个适配器遵守，所以同一个错误不会再从新
  交易所回来。
- **事件模式新增 `Event::Depth`(kind 9)，且 `Event` 不再是 `Copy`。**
  *会改变 L2 运行的结果，且只改变这类运行。* 一次 L2 运行的成交取决于它的订单
  排在哪本簿里，而没有簿的日志会把这些订单重放进另一个市场——重现的是「当初请求
  了什么」而不是「当时发生了什么」。这是这个内核里唯一一处重放不忠实的地方，现在
  不是了。**行为差异：** 对不读深度的 L0 与 L1 没有任何变化；一次 L2 重放现在
  重现的是它自己那次运行的成交，而不是更低档位的答案。磁盘上是增量的：此前写下的
  日志里没有这类记录，所以重放旧日志不变；而带着它写下的日志无法被更早的版本读取
  ——按 kind 的长度校验会让这件事报错而不是静默。**这是第一个变长的 kind。**
  其余每一种载荷都是定长的、decode 拒绝任何别的长度；深度更新自带档位计数，
  decode 用字节数去核对它——同一条规则，只是从对着常量改成对着声明。`Copy` 之所以
  去掉，是因为一列档位不是 `Copy`；需要改的六处调用都是机械的。
- **事件模式新增 `Event::VenueFill`(kind 8)。** 表示"由交易所决定的成交",区别于
  撮合器产生的成交。**增量式**:此前写下的 journal 里不含这种记录,重放不变。而含它
  的 journal 无法被旧构建读取——这正是逐 kind 精确长度检查存在的意义:让它响,而不是
  让它无声。
- **新增 `State::matching`,默认 `Matching::Simulated`。** `Venue` 模式下撮合器持有
  挂单但绝不成交,成交由 `VenueFill` 送入。默认值就是既有行为,现存运行全部不变。
- **新增 `RejectReason::NotVenueMatched`。** 交易所成交到达模拟内核时被拒绝而非应用。
- **`oq_gateway::OrderUpdate` 新增 `side` 与 `maker`。** 交易所一直在发,解析器一直在
  丢。`maker` 决定手续费,在某些交易所是返佣与收费之别。缺省即 taker。
- **`binance::classify` 现在对响应是全函数**,2xx 交由 `ack_from` 处理。它的契约面
  原本是一对函数,而 OKX 是一个,导致没有任何单一一致性套件能同时驱动两者。`place`
  不受影响——它从不把 2xx 交给这个函数。
- **`RunResult::margin_usage` 是新增字段**;`RunConfig::track_margin` 默认关闭,既有
  运行既不改变也不为它付出代价。
- **`Record::Submitted` 末尾新增 `leg` 字段,`Belief` 按腿重建持仓。** *改变重建出来的结论,不改变运行本身。*
  对冲账户原来被重建成一个净额:多 0.004、空 0.008 读回来是空 0.004,于是每次对冲账户的对账都与交易所
  不一致。净额无法直接拆开:对冲账户上 reduceOnly 标记会被去掉(交易所不接受),没有腿信息就分不清是平空
  还是开多。旧 journal 仍可读,腿未知;腿未知的对冲成交记为无法解读,不去猜。记录比较也不再把浮点显示差异
  (`83794.9` 对 `83794.90000000001`)当成不同,并把单向持仓的 `BOTH` 按方向对应。
- **新增 `Record::Operator`(实盘 journal kind 10)和本机控制口。** *只新增记录类型,不改已有记录。*
  在 systemd 下带运行目录启动的进程,会在那里监听一个 Unix socket,接受 `status`、`orders`、
  `metrics`、`attribution`、`halt`、`shutdown`、`resume`,命令在主循环里像其他事件一样处理。对端按内核报告的
  uid 放行;没有运行目录就不开控制口,绝不退回 `/tmp`。每个改变状态的命令都连同原因、已认证的
  来源和结果写进 journal。`resume` 默认关闭,需以 `--control-allow-resume` 启动,且在最近一次
  持仓核对不一致或 journal 无法记录时拒绝。操作者要求的干净退出以 98 退出,供 supervisor 不再拉起。
- **新增 `Record::Cancelled`(实盘 journal kind 9),`Belief::from_journal` 会把它
  减掉。** *改变重建出来的结论,不改变运行本身。* 实盘 journal 记下了提交、结果和成交,
  唯独交易所撤单时什么都不记。于是一笔「已接受、未成交、没有结局」的挂单,重建时只能被
  读成仍然挂着:在一次真实部署上,这表现为**重建出的挂单数是账户实际持有的数倍**,
  中间差的全是 journal 当时没有记录类型可装的撤单。[切换手册](docs/CUTOVER.zh-CN.md)第 5 步
  正是拿 `oq-belief` 去比对的,所以这个错误是承重的。**行为差异:**对任何运行都没有——
  这条记录写在交易所确认撤单的地方,循环里没有任何东西会把它读回来。经 `oq-belief` 或
  `oq-replay` 重放的 journal,现在报出的挂单是交易所认得的那些。磁盘上是追加式的:之前
  写下的 journal 里没有这种记录,重放不变;而带着它写下的 journal,旧版本会逐帧跳过而
  不是读错——显式编号 kind 就是为这件事存在的。

### 文档

- `sweep_100` 基准跑在 `MarketShape::trending(600_000)` 上,而它的漂移是逐观测复利的:
  末价正好顶到 `i64::MAX`。**这道门打印过的每一个统计量,都是在饱和价格上做的算术**,
  包括它自己输出里那个 PBO 0.4975。现在它跑在平静市场上,并且断言它拿到的市场。没有
  任何文档引用过那些数字,所以别处无需改动。

### 引擎

- `oq-types` —— 领域类型、`i64` 定点运算、订单与仓位的 typestate 状态机。
- `oq-hash` —— SHA-256 与 CRC-32，由 journal、采集、parity 共用。
- `oq-journal` —— 追加日志，含快照、重放与撕裂尾容忍。
- `oq-core` —— 先落盘的定序器与确定性内核。重放能逐位复现输出序列与最终账户状态，
  由测试断言，含一条强平路径。
- `oq-engine` —— L0 tick 回放撮合，含跳空成交、价格改善、价格时间优先。已冻结为
  回归锚点。跨跳空的成交以**前一个** tick 打时戳。
- `oq-margin` —— 分级维持保证金、强平价由推导得出而非照抄、资金费与尖峰注入、
  规则表双时间存储。
- `oq-backtest` —— 回测宿主、策略可观察自己的成交，以及保证金偏差报告：同一策略
  跑两遍，量化无保证金那一臂虚报了多少。
- `oq-data` —— 双时戳 tick、无泄漏 as-of join、双时间参考数据。Tick 文件改为流式
  读取而非整体缓冲。`.oqtk` 格式为 v2；tick 携带成交量。
- `oq-parity` —— 逐笔运行比对与差异归因；基线由（commit、数据哈希、配置哈希）
  三元组标识，因此过期的基线会自报过期，而不是伪装成一次回归。
- `oq-stats` —— Deflated Sharpe Ratio、经 CSCV 的 PBO、试验登记。

**经典策略目录的加杠杆数字变了。** *改变结果。* `GridTrader` 原来在**提交**
一格时就推进梯子;现在改为在**成交**时推进,并且锚在实际成交价上,同时最多
一格在途。网格的加杠杆结果从 4.06 变成 4.46,margin-free 那一侧从 −513.74
变成 −508.12。QUICKSTART 两个语言版本都引用了这两个数,而它们变的时候**没有
任何测试失败**——这个目录从来没有被 `tests/golden.rs` 钉住过。现在钉住了:
每一行加杠杆的数字,外加文档真正主张的那句话——不加杠杆时六个策略的两列都
一致。把 4.06 放回去验证过,失败信息是 `expected 4.06, got 4.46`。

- **`L2Engine` —— 排队位置与吃单成本都从交易所的订单簿读取。** 它包住 `L1Engine`，
  一如后者包住 `L0Engine`，所以 L0 的冻结由构造保证。订单加入的那一档实际挂出的量
  就是它前面的队列，取代 L1 假设的 `QueueAhead`；吃单则逐档走完盘口、付这段路的
  加权价，取代 L1 的平方根惩罚。实测**替代** policy 而不与之叠加，盘口够不到的那笔
  单保留 policy，`swept` / `unswept` 说明一次运行到底是被哪一个定的价。盘口与 tick
  冲突时取更差的价，所以往上爬一档永远不会让回测更好看。**目前还没有任何东西在喂它**
  ——tick 文件里只有最优买价和最优卖价，没有订单簿。*（此后已被取代：下面的
  `run_observations` 喂它，`oq-tiers` 从采集归档喂它。）*
- **`oq-book` —— 订单簿重建独立成 crate**，从 `oq-l2feed` 中抽出，好让撮合器不必
  为了看一眼价位而继承一整套 TLS 栈。`oq-l2feed` 在原路径重导出，没有一处调用点需要改。
- **回测可以对着重建出的深度撮合了。** `RunConfig::at_tier` 选择 L0、L1 或 L2，
  `run_observations` 接收同一条流上的 tick 与深度更新——之所以是一条流，是因为两条
  就意味着由调用方来写那次归并，而顺序错了的归并会让一张单去撮合一本来自未来的簿。
  快照是它自己的一种到达：增量流说的是「变了什么」，而一本没有东西可变的簿会拒绝
  每一条更新。`RunResult` 新增 `tier` 与三个深度计数，因为没有指明撮合器的成交是
  没有出处的数字，而把深度交给一个不读它的档位，等于一次运行用更高档位的名字报出
  了更低档位的答案。还有两个缺口留在路线图里：把归档转成这条流仍是调用方的循环；
  深度不进日志，所以一次 L2 运行的日志能重放它的订单、重放不了那本簿。*（此后两个
  缺口都已补上：`oq_ingest::fold_into_observations` 转换归档，上面的 `Event::Depth`
  把簿放进了日志。）*
- **`oq-core` 新增 `Matcher`** —— 内核持有一个档位，而不是它自己就是一个档位。
  `State.engine` 此前按名字就是 `L0Engine`，这正是没有任何东西能够到 L2 的原因。
  用枚举而不是 trait：档位是一个封闭集合、分派在热路径上、且快照必须能说出自己
  恢复成了哪一个。在干净的 runner 上吞吐没有变化，14.64 → 14.67 M ticks/s。
- **`limit_order` 与 `market_order` 抽成自由函数**，这样一个档位无法用与它下层
  不同的方式构造订单。
- **`oq-data` 会报出风格化事实**，于是"这份数据像不像一个市场"变成一条命令而不是一项
  研究。四天采集的 BTCUSDT 每天四条成立三条、超额峰度 8–11，而生成夹具是
  0.03 / 0.07 / −0.05。

### 实盘交易

这里没有任何东西交易过真钱，[路线图](docs/ROADMAP.zh-CN.md) §M3 的准入
条件写明了那之前必须先成立的事。

- `oq-gateway` —— 两家交易所的执行适配器*（此后增至八家：Binance、OKX、Aster、
  Kraken Futures、Deribit、Hyperliquid、Backpack、Bitget——见[交易所](docs/VENUES.zh-CN.md)）*。下单结果是三态：接受、拒绝、
  以及**未知**——未知不是错误，因为错误会让调用方用 `?` 一带而过，而那
  恰恰是唯一一个必须被处理的情形。一致性测试套件用同一组用例驱动两个
  适配器，套件自身则用三个故意写错的适配器来检验*（现在它驱动六个适配器；Aster
  与 Binance 共用一个）*。`broker::IdScheme`
  组装带交易所返佣码的 client id，与"这单是不是我的"那个前缀分开。
- `oq-risk` —— 盘前闸门、急停、启动对账。`VersionedLimits` 记录哪一项
  限额变了、从什么变成什么；一次什么都没改的变更不会推进版本号。
- `oq-live` —— 进程装配、快照恢复，以及**用回测同一个内核**记的账，所以
  只有一份账本实现，而不是两份"一直吻合直到不吻合"的实现。启动时与交易所
  对账，发现旁边有没人告诉过它的仓位就拒绝运行；并且用同一批事件跑影子回测，所以
  每次运行结束都会按成因分解差距，并保留一个明确“无法解释”的残差。
  手续费取自交易所自己的成交记录,经 `Account::fees_charged`,其默认实现返回 `None`
  ——没有实现它的适配器会**说出来**,而不是答零,因为零是一个测量值。资金费仍然
  “不可得”:交易所把它放在没有任何适配器读的端点上。残差扛着缺失的部分,报告会指名。
- metrics 是一个**快照值**，按采集器读的那种行式格式渲染；alert 是判断
  而不是通知：本工作区里没有任何东西会向任何地方发送任何东西。
- **一次实盘运行会留下运行文件和一个 tick 文件。** 每十五分钟和退出时，它在 journal
  旁边写出 `<stem>.live.run` 与 `<stem>.model.run`——交易所的成交与影子的成交，同一个
  身份，`oq-parity` 读的格式——以及 `<stem>.oqtk`，即观测本身，供 `oq-parity markout`
  使用。每个文件都先写到旁边再改名，所以读者永远看不到半个文件。控制端口在运行中用
  影子的证据回答 `attribution`。
- **影子看到每张单被发出时的样子，也看到每一次撤单。** *改变影子报告的内容。* 此前只有
  tick 路径会把订单告诉影子，而且发出去的每张单都不带价格，于是一架挂着的梯子到了模型
  那里变成市价单、立即成交；被暂停或被交易器自己撤掉的单，在模型里仍然挂着，之后还可能
  成交。它的成交——以及由此算出的每一条分歧和归因——描述的是交易所从未持有的订单。
  现在每一种结果都经过同一个函数，同时告诉账本和影子，包括由成交触发下的单和交易所
  结束的单。回测不受影响。
- **控制端口的 `status` 说得更多了：** 合约的 `price_scale` 与 `qty_scale`，让读者能用
  交易所的单位显示订单；一个 `pnl` 对象（自运行开始以来的已实现盈亏、手续费、资金费、
  净额和权益）；以及一个 `limits` 对象，列出本次运行所受的风控限额。
- **`oq-recon --watch --latest FILE`** 把最新一次读数按 `--record` 的格式保存下来，每次
  读取后原子地重写，于是对照交易所核对 journal 不再需要有人去粘贴一份记录。

**`Outcome::Unresolved` 从 `Outcome::Refused` 中拆出。** *改变实盘行为。*
一次发出去了、但从未收到答复的提交，原来会以 `accepted = false` 报给策略
——这是在告诉策略"这单不存在"，而它可能正挂着，于是招来那个唯一能把
"也许有一单"变成"确定有两单"的动作。未答复的提交现在是它自己的一种结果，
并且**根本不经过** `Strategy::on_placed`。控制台输出与收尾汇总也把两者
分开。回测结果不受影响：模拟撮合对每一次提交都会答复。

**新增 `Strategy::on_placed`**，默认空实现，回测循环与实盘宿主两边都调用。
一个把"我要了"当成"它挂上了"的策略，相信自己持有并不存在的敞口。

### 手续费

Maker/taker 手续费在内核中计提。maker 费率**可以为负**——返佣是存在的，而一个在零处
截断的模型无法表达靠返佣生存的那类策略。**费率默认为零，必须显式设置**——示例都没有
设置，所以文档引用的每个数字都是不含成本的毛数。

### 采集

- `oq-l2feed` —— 原样分帧、按 UTC 日轮转、带内容哈希 manifest 的密封。能扛过日界；
  按定时器 flush，而不只按记录条数。
- `oq-capture` —— Binance 永续流的实盘客户端*（此后也支持 OKX 永续，用 `--venue`
  选择）*，采集**交易所实际提供**的那些流
  （有些接受订阅之后什么都不发，见[采集格式规范](docs/CAPTURE-FORMAT.zh-CN.md)）。
- keepalive 每转一圈把控制权交回采集循环，不再在 source 内部空等。source 现在自己声明
  "静默算不算断线"，带 keepalive 且对端应答的连接答"不算"。在读里空等会让连接活着而循环
  睡着，静默流看不见自己的关闭标志——实盘上实测为收到 SIGTERM 后三分钟仍在运行。
- 控制记录写完即 flush。采集循环只在"收到消息"时 flush，于是一条什么都没收到的流，
  它的 session 标记会无限期留在缓冲区里，磁盘上的文件看起来像这条流从未启动过。
- 用 manifest 校验实盘归档时发现并修掉的采集问题：静默属于常态的流现在自带
  keepalive，不再每次读超时就重连；gap 标记记录**实测的断线时长**，而不是连接
  已保持的时长；轮询载荷用它自己的时间戳作为事件时间；文件里没有该时钟的记录时，
  manifest 写 `null` 而不是纪元 0。最后一条是 **manifest 结构变更**——把这四个
  字段按数字解析的读者需要接受 `null`。
- `oq-book-check` —— 把归档重放回订单簿，报告它能否重建。磁盘上的字节只能证明消息
  到过；只有重建能证明它们可用。这是**归档校验**，**不是**保真度的 L2 档。
- **压缩后的归档现在不只能读，还能被找到。** 早先的改动教会了每个工具读
  `.oqcap.zst`，却没有教它们**找**到一个：`oq-ingest` 按扩展名过滤，而压缩文件没有
  那个扩展名——于是整整一天的采集报告"没有东西可转"，读起来和空目录一模一样。命名
  现在统一走 `archive::stem`，与 `archive::read` 放在一起。
- **`pull-capture-cron.sh`** —— 归档拉取里"定时任务需要而手工运行不需要"的那一半。
  被锁挡住时退出码是 `3`，不再与传输失败共用 `1`：首次补拉必然长于任何合理的间隔，
  重叠是一个正在追赶的归档的正常状态，而分不清这两者的监控，要么在正常重叠时误报、
  要么在真丢数据时沉默。它在归档根目录留下 `.last-success` / `.last-failure`，因为
  这里的失效模式是沉默，而没人读的日志就是沉默。

### 示例与性能

- `oq-examples` —— 跑在 seed 固定的合成行情上的教学示例，`tests/golden.rs`
  钉住文档引用的每一个数字。另附一个经典策略目录——RSI、MACD、布林、
  唐奇安、网格、Dual Thrust——用它们公开发表的参数，未做调优。每一个都有
  几十年历史、被足够多的人交易过，它曾经有过的优势不会等在一个公开仓库里；
  它们在这里，是为了让人可以靠“认出某样东西”来学这个框架。每一个都写明
  自己在哪里失效，而不是宣称有优势。
- **参数扫描的结果成为一个文件。** `oq_backtest::sweep_file` 把一次扫描——每个配置的
  结果、旁边的 deflated Sharpe 与 PBO、拒绝理由，以及任何没能算出的统计量的原因——
  渲染成以 `openquanter-sweep 1` 开头的行式格式，并能读回来。`sweep_100 --out FILE`
  会写出一个。见[扫描文件格式](docs/SWEEP-FORMAT.zh-CN.md)。
- `criterion` 基准，以及一个断言吞吐**下限**（而非被跟踪基线）的 CI job。共享 runner
  在不同时段能差好几倍，一个会因噪声误报的门迟早被关掉；精确比较是在同一台机器上跑
  `cargo bench`。

### 项目

- 全工作区一个版本，在根 `Cargo.toml` 里声明一次。见[版本规则](docs/VERSIONING.zh-CN.md)。
- CI：构建、测试、`fmt --check`、`clippy -D warnings`、对工作区与历史的秘密与部署
  细节扫描、依赖预算与独立构建检查（`scripts/check-composability.sh`）、吞吐下限。
- 强制 DCO sign-off；实质性贡献需 CLA。
- 中英双语文档：需求、路线图、实施方案、快速上手、版本规则、采集格式、Tick 格式。
